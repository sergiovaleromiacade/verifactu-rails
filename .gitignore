/.bundle/
/pkg/
/tmp/
Gemfile.lock

# doc/ SÍ se versiona: contiene FUENTES.md, no documentación generada.
# Los PDF de la AEAT no, que pesan y no diffean.
/doc/*.pdf
*.gem

# Nunca versionar credenciales del obligado tributario.
*.p12
*.pfx
*.pem
*.key

.DS_Store
/.ruby-lsp/

# Diario de las campañas contra preproducción: lleva el NIF del obligado, los CSV
# de la AEAT y las huellas de una cadena concreta. Es un registro de ejecución,
# no fuente.
/campana-*.jsonl
