# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

group :development, :test do
  gem 'minitest', '~> 5.0'
  gem 'rake', '~> 13.0'

  # Todavía solo de test: valida los XSD oficiales. Pasará a dependencia de
  # runtime en el gemspec cuando exista el generador de XML.
  gem 'nokogiri', '~> 1.15'
end
